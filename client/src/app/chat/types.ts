type userDataType = {
  email: string;
  username: string;
  status:string,
  bio:string
};

type eachMessageType = {
  senderEmail: string;
  senderUsername: string;
  messageText: string;
  timestamp: string;
  conversationId: string;
};
